---
name: chai-fen-sao-miao
description: |
  小说拆分扫描工具。对给定的小说文件进行结构扫描和章节拆分。
  当用户提到"拆分扫描"、"扫描小说结构"、"拆分章节"时使用。
---

# 拆分扫描

小说拆分扫描工具。对给定的小说文件进行结构扫描和章节拆分。

## 使用方式

用户提供小说文件路径，执行以下流程：

1. **扫描阶段**：调用 Python 脚本扫描小说结构
2. **报告阶段**：展示扫描结果，询问用户拆分粒度
3. **拆分阶段**：按用户指定的层级执行拆分
4. **完成报告**：展示拆分结果

## 执行步骤

### 步骤1：扫描结构

执行命令：
```bash
python3 {WORKSPACE}/novel_splitter.py --scan "文件路径"
```

### 步骤2：展示报告并询问用户

根据扫描结果，向用户展示检测到的章节层级，询问：
- 希望按哪个层级拆分？（卷/章/节/话等）

### 步骤3：执行拆分

执行命令：
```bash
python3 {WORKSPACE}/novel_splitter.py --split [层级] "文件路径"
```

### 步骤4：报告完成

告知用户：
- 输出目录位置
- 生成的文件数量
- 是否有异常情况

### 步骤5：同步至 AXIS 规范路径

「输出目录结构」为脚本原生输出；拆分完成后，须将本次拆分产物同步至 AXIS 视频制作层规范路径，供 `short-drama-architect` 等下游直接消费：

- 目标路径：`assets/novel-analysis/{书名}/拆分/`（{书名} 不带书名号，与项目目录一致）
- 操作：先按下文「拆分 MD front matter 规范」为每个章节 MD 补写头部元信息，再将全部章节 MD 复制/移动到该目录（目录不存在则创建）
- 同步后向用户报告 AXIS 规范路径，便于后续 `~outline` / `~full` 直接读取

## 输入参数

用户输入 - 小说文件路径（支持 txt、epub、md 格式）

## 输出目录结构

```
Factory/拆书/{书名}/
├── {书名}.txt           # 源文件（拆分后自动移入）
├── 拆分/
│   ├── 001_第一章_xxx.md
│   ├── 002_第二章_xxx.md
│   └── ...
├── 概括/                # 后续概括生成
├── 人物和设定/          # 后续分析生成
└── 全书概括.md          # 后续概括生成
```

## 拆分 MD front matter 规范

拆分生成的每个章节 MD，须在文件顶部写入 YAML front matter，便于下游（分章节概括 / `short-drama-architect`）快速读取章节元信息：

```yaml
---
章节序号: 001          # 与文件名序号一致，三位补零
字数: 3200             # 本章正文字数（不含 front matter）
对话密度预估: 中        # 高 / 中 / 低，按对话行占比估算（高≥40%，中 15-40%，低<15%）
---
```

## 注意事项

- 如果未检测到标准章节结构，提示用户可能需要手动指定或检查文件
- 无法识别的内容会合并到前一个已识别章节
- 支持的拆分层级：卷、部、篇、章、集、回、话、节
- **项目目录命名**：统一使用 `{书名}/` 格式（不带书名号），与源文件名保持一致
- **源文件处理**：拆分完成后，源文件自动移动到项目目录下，保持与目录同名
- **防嵌套机制**：
  - 脚本会自动检测并避免创建嵌套目录结构
  - 如果检测到已存在的嵌套目录（如 `书名/《书名》/`），会自动合并到父目录
  - 支持检测和清理带书名号的嵌套目录（《》、「」等）

---

## 诊断指南

| 问题 | 检查 | 修复 |
|------|------|------|
| 未检测到章节结构 | 检查文件格式和编码 | 提示用户手动指定拆分层级 |
| 拆分后文件为空 | 检查层级选择是否正确 | 尝试更细粒度的层级 |
| 嵌套目录 | 检查是否存在书名号嵌套 | 脚本自动处理，也可手动移动 |
| 文件格式不支持 | 检查是否为txt/epub/md | 转换为支持的格式 |

## 术语表

| 术语 | 定义 |
|------|------|
| 拆分扫描 | 对小说文件进行结构识别和章节拆分 |
| 拆分层级 | 卷/部/篇/章/集/回/话/节等结构单元 |
| 防嵌套机制 | 自动检测并避免创建嵌套目录 |
| 源文件 | 用户提供的原始小说文件 |

## 质量标准

| 检查项 | 标准 |
|--------|------|
| 章节识别 | 正确识别所有章节分隔符 |
| 文件完整 | 拆分后无内容丢失 |
| 命名规范 | 文件名包含序号+章节名 |
| 无嵌套 | 目录结构所叙平整，无重复嵌套 |
